<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head"); %>
  </head>
  <body>
    <div class="container">
      <div class="sidebar"><%- include("partials/sidebar"); %></div>
      <main>
        <h1><%= title %></h1>
        <% if (category_items.length > 0) { %>
        <p>
          <strong
            >Delete the following items before attempting to delete the
            category: <%= category.name %></strong
          >
        </p>
        <dl>
          <% for (let i = 0; i < category_items.length; i++) { %>
          <dt>
            <a href="<%= category_items[i].url %>"
              ><%= category_items[i].name %></a
            >
          </dt>
          <dd><%= category_items[i].description %></dd>
          <% } %>
        </dl>

        <% } else { %>
        <p>
          <strong>
            Are you sure you want to delete the category: <%= category.name
            %>?</strong
          >
        </p>
        <form method="post" action="">
          <div class="form-group">
            <input
              id="categoryid"
              class="form-control"
              type="hidden"
              name="categoryid"
              required="true"
              value="<%= category._id %>"
            />
          </div>
          <button class="submit-button" type="submit">Delete</button>
        </form>
        <% } %>
      </main>
    </div>
  </body>
</html>
