<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head"); %>
  </head>
  <body>
    <div class="container">
      <div class="sidebar"><%- include("partials/sidebar"); %></div>
      <main>
        <h1><%= title %></h1>
        <h2><%= item.name %></h2>
        <div class="image-wrapper">
          <img class="item-image" src="<%= item.uploaded_file %>" />
        </div>
        <p><strong>Description:</strong> <%= item.description %></p>
        <p><strong>Category:</strong> <%= item.category.name %></p>
        <p><strong>Price:</strong> <%= item.price %></p>
        <p><strong>In Stock:</strong> <%= item.number_in_stock %></p>
        <div class="button-group">
          <button
            class="update-button"
            onclick="location.href=`<%- item.url + '/update' %>`"
          >
            Update
          </button>
          <button
            class="delete-button"
            onclick="location.href=`<%- item.url + '/delete' %>`"
          >
            Delete
          </button>
        </div>
      </main>
    </div>
  </body>
</html>
